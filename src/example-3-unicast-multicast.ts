import {Observable} from 'rxjs';

/*const myUniCastObservable = new Observable(observer => {
	observer.next('my value')
});*/

/*
const myValue = 'myValue';
const myMultiCastObservable = new Observable(observer => {
	observer.next(myValue);
});
*/
